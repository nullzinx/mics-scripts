# telegram scripts

## export_private_chat.py
script for exporting the conversation history of a private Telegram group
Arguments
- "-c", "--chat" — Target chat ID
- "-o", "--output" — Output file where the conversation will be saved

Example
```bash
python export_private_chat.py -c "-1003720662242" -o mensagens.txt
```
